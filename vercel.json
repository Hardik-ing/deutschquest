{
  "version": 2,
  "public": true,
  "builds": [
    {
      "src": "deutschquest-frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/deutschquest-frontend/$1"
    }
  ]
}